# Tạo mới một user trong centos

 * `sudo adduser sammy`
 * `sudo passwd sammy`
 * `sudo usermod -aG wheel sammy` (đây là lệnh add user vào group => user này có quyền chạy sudo, được add vào group wheel)